<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.OverflowText" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_dark</item>
        <item name="colorPrimaryVariant">@color/primary_dark_variants</item>
        <item name="colorOnPrimary">@color/on_primary_dark</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/primary_dark</item>
        <item name="colorSecondaryVariant">@color/primary_dark</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="backgroundApps">
        <item name="android:background">#C4C4C4</item>
    </style>

    <!-- style untuk teks header -->
    <style name="headerText">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">24dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">@font/font_quicksand_semibold</item>
    </style>

    <!-- style untuk teks body -->
    <style name="bodyText">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">16dp</item>
        <item name="android:letterSpacing">0.032</item>
        <item name="android:fontFamily">@font/font_questrial_regular</item>
    </style>

    <!-- style untuk teks body -->
    <style name="descText">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">12dp</item>
        <item name="android:letterSpacing">0.032</item>
        <item name="android:fontFamily">@font/font_questrial_regular</item>
    </style>

    <!-- style untuk line -->
    <style name="lineVertical">
        <item name="android:background">@color/white</item>
    </style>

    <!-- style untuk background Button/CardView -->
    <style name="backgroundButton">
        <item name="android:background">@color/primary_dark</item>
    </style>

    <!-- style untuk membuat style pada Toolbar -->
    <style name="appBarStyle">
        <item name="titleTextColor">@color/white</item>
        <item name="navigationIconTint">@color/white</item>
        <item name="titleTextAppearance">@style/TextAppearance.AppCompat.Title</item>
    </style>

</resources>